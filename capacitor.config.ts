import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ApiRestPrevision',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
